<view>
    <view class="page__bd">
        <view class="weui-cells__title">路径：{{path}}</view>
        <view class="weui-cells weui-cells_after-title">
            <view class="weui-cell" wx:for="{{displayItems}}" bindtap="click" data-file="{{item}}">
                <view class="weui-cell__bd">{{item.name}}</view>
                <view class="weui-cell__ft">{{item.readableSize}}</view>
            </view>
        </view>
    </view>
</view>